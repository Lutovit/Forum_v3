@if (User.Identity.IsAuthenticated && User.IsInRole("admin"))
{
    @Html.ActionLink("Панель администратора", "Index", "Admin", routeValues: null, htmlAttributes: new { id = "adminLink", @style = "color:white" })
}
